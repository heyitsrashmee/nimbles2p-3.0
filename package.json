{
  "name": "nimbles2p-website",
  "private": true,
  "scripts": {
    "predev": "node scripts/prepare-public.mjs",
    "clean": "rm -rf dist .next",
    "dev": "next dev",
    "dev:clean": "npm run clean && npm run dev",
    "prebuild": "node scripts/prepare-public.mjs",
    "build": "next build",
    "start": "next start",
    "verify": "node scripts/prepare-public.mjs && node scripts/verify-assets.mjs"
  },
  "dependencies": {
    "lucide-react": "^1.16.0",
    "next": "^15.1.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
