"use client";

import GatedResourceDownloadPage from "@/components/GatedResourceDownloadPage";

/** Gated guide / PDF — per-resource download form (Get Started layout). */
export default function GatedResourcePage(props) {
  return <GatedResourceDownloadPage {...props} />;
}
